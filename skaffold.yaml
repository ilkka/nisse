apiVersion: skaffold/v4beta2
kind: Config
metadata:
  name: nisse
build:
  artifacts:
    - image: nisse
      docker:
        dockerfile: Dockerfile
    - image: nisse-dev
      docker:
        dockerfile: Dockerfile
        target: build
manifests:
  rawYaml:
    - k8s/db.yaml
    - k8s/web.yaml
